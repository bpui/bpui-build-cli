```bash
bpui-build router2 [layout=layoutName]       # Build router config file.

# set BPUI_ANALYZER=1 to enable function：
#     webpackConfig.initBundleAnalyzer
BPUI_ANALYZER=1 vue-cli-service build
```

use `webpack@4`